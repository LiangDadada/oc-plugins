<!-- Managed by oc-plugins/opencode-loop -->
---
description: Alias for /loop-optimize
agent: loop-builder
---

Run the same protocol as `/loop-optimize` for this task:

$ARGUMENTS

Load the `software-loop-contract` skill, create or continue an optimization loop, baseline before changes, make one coherent candidate per attempt, record metrics, and accept only if the configured threshold and health checks pass.
