<!-- Managed by oc-plugins/opencode-loop -->
---
description: Alias for /loop-tdd
agent: loop-builder
---

Run the same protocol as `/loop-tdd` for this task:

$ARGUMENTS

Load the `software-loop-contract` skill, create or continue a TDD loop, follow `INTAKE -> SPEC -> RED -> GREEN -> REFACTOR -> REVIEW -> ACCEPT -> DONE`, record spec/red/green evidence, and do not mark `DONE` until `loop-acceptor` confirms acceptance.
